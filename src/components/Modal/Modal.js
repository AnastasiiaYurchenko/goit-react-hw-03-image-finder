import { ModalContent, Overlay } from './Modal.styled';

export const Modal = () => {
  return (
    <Overlay>
      <ModalContent>
        <img src="" alt="" />
      </ModalContent>
    </Overlay>
  );
};
